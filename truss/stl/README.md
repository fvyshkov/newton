# STL — truss-вариант

Здесь будут лежать новые печатные детали под truss-конструкцию. Пока пусто — генерация после подтверждения размеров стоек и геометрии.

## Список печатных деталей

| Файл | Кол-во | Назначение |
|---|---|---|
| `Mirror_box.stl` | 1 | нижняя клетка-короб 290×290мм, 3 отверстия под шпильки M8 |
| `Truss_socket.stl` | 8 | клипса-зажим под стойку (4 на mirror box + 4 на UTA) |
| `Cell_leg_bracket.stl` | 3 | скоба-ножка, на ухе старой оправы, спускается к шпильке M8 |
| `UTA_ring.stl` | 1 | верхнее кольцо ID 240мм с 4 сокетами + посадка фокусера |
| `Spider_4vane.stl` | 1 | 4-лопастной паук под ID UTA 240мм |
| `Secondary_holder.stl` | 1 | держатель вторички с коллимацией |
| **Итого** | **15** | — |

## Что НЕ печатаем (берём из основного проекта)

Эти STL уже напечатаны или будут перепечатаны без изменений из `/Users/mac/newton/stl/`:

- `Mirror_cell_front.stl` + `Mirror_cell_back.stl` — оправа целиком
- Фокусер: `Focuser_inner`, `Focuser_outer_top`, `Focuser_outer_bottom`, `Focuser_ring`, `Collet`, `Collet_nut`, `1.25_eyepiece_adapter`, `Focuser_grub`, `Focuser_grub_screw_panel`

## Геометрия (контракт)

Все детали проектируются под единые параметры — менять только в одном месте, иначе детали не сойдутся:

```
TUBE_DIAM_MIN = 22.0    # мм, минимальный диаметр стойки (зажим)
TUBE_DIAM_MAX = 28.0    # мм, максимальный
UTA_ID = 240.0          # мм, внутренний диаметр верхней клетки
MIRROR_BOX_INSIDE = 250 # мм, внутренний размер нижней клетки (квадрат)
TRUSS_LENGTH = 900.0    # мм, расчётная полезная длина стойки
HEIGHT_ADJUST_RANGE = 40 # мм, ход регулировки высоты оправы
```

После генерации все скрипты будут в этой же папке (`generate_*.py`).
